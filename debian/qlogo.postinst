#! /bin/sh

set -e

case "$1" in
    configure)
        # Alternatives
        update-alternatives --quiet \
            --install /usr/bin/logo logo /usr/bin/QLogo 20
        update-alternatives --quiet \
            --install /usr/bin/logo logo /usr/bin/QLogo-cli 15
        ;;
esac

#DEBHELPER#
