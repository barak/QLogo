#!/usr/bin/make -f

export QT_SELECT = 6
export DEB_BUILD_MAINT_OPTIONS = hardening=+all

%:
	dh $@

execute_after_dh_auto_install:
	mkdir --verbose -p debian/qlogo/usr/bin
	mv --verbose debian/qlogo/usr/qlogo debian/qlogo/usr/bin/
	mv --verbose debian/qlogo/usr/QLogo-GUI debian/qlogo/usr/bin/
